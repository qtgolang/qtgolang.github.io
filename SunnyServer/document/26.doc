.版本 2
.支持库 spec

.子程序 __启动窗口_创建完毕
.局部变量 a, Sunny队列

' 可以多线程使用，是线程安全的
a.创建队列 (“我是唯一ID”)  ' 同一个唯一ID 第一次必须创建列队
a.压入 (“0”)
a.创建队列 (“我是唯一ID”)  ' 重复创建会先清空
a.压入 (“1”)
a.压入 (“2”)
a.压入 (“3”)
调试输出 (a.弹出 ())  ' 输出1
调试输出 (a.弹出 ())  ' 输出2
子程序1 ()
调试输出 (a.弹出 ())  ' 输出4
调试输出 (a.弹出_字节集 ())  ' 输出 "5"的字节集{53}
' 压入的文本 可以弹出为字节集 压入的字节集可以弹出为文本
a.清空 ()
调试输出 (a.弹出_字节集 ())  ' 输出 "" 的字节集{}

a.压入_字节集 (到字节集 (“123456”))

子程序2 ()
结束 ()


.子程序 子程序2
.局部变量 c, Sunny队列

c.置唯一标识 (“我是唯一ID”)
调试输出 (c.弹出 ())  ' 输出123456

c.销毁 ()  ' 这个唯一ID 不再使用时 销毁

.子程序 子程序1
.局部变量 b, Sunny队列

b.置唯一标识 (“我是唯一ID”)
调试输出 (b.弹出 ())  ' 输出3
b.压入 (“4”)
b.压入 (“5”)
b.压入 (“6”)